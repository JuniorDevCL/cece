# Copia este archivo como .env.local para desarrollo local

# Neon conectado en Vercel — usa la variable que Vercel crea automáticamente
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

# Auth.js — OBLIGATORIO en Vercel. Sin esto /api/auth responde "Bad request."
AUTH_SECRET="reemplaza-con-un-secreto-largo"

# URL pública de la app (sin barra final)
# Local:
AUTH_URL="http://localhost:3000"
# Producción (ejemplo):
# AUTH_URL="https://cece-eight.vercel.app"
